{
  "entities": {
    "receptionEntry": {
      "title": "Reception Patient Entry",
      "description": "Patient registration entry created at reception with billing and test order details",
      "type": "object",
      "properties": {
        "id": { "type": "string" },
        "tokenNumber": { "type": "string" },
        "labId": { "type": "string" },
        "patientName": { "type": "string" },
        "age": { "type": "number" },
        "gender": { "type": "string" },
        "phone": { "type": "string" },
        "tests": { "type": "array" },
        "totalAmount": { "type": "number" },
        "paidAmount": { "type": "number" },
        "paymentStatus": { "type": "string" },
        "status": { "type": "string" },
        "createdAt": { "type": "string" },
        "updatedAt": { "type": "string" }
      },
      "required": ["id", "labId", "patientName", "totalAmount"]
    },
    "labReport": {
      "title": "Lab Test Report",
      "description": "Clinical pathology test report with diagnostic parameters and pathologist signature",
      "type": "object",
      "properties": {
        "reportId": { "type": "string" },
        "labId": { "type": "string" },
        "patientId": { "type": "string" },
        "patientName": { "type": "string" },
        "testName": { "type": "string" },
        "status": { "type": "string" },
        "results": { "type": "array" },
        "published": { "type": "boolean" },
        "updatedAt": { "type": "string" }
      },
      "required": ["reportId", "labId", "patientName", "testName"]
    },
    "vendorBooking": {
      "title": "Home Collection Booking",
      "description": "Online booking submitted by patients or reception for home sample collection",
      "type": "object",
      "properties": {
        "id": { "type": "string" },
        "labId": { "type": "string" },
        "patientName": { "type": "string" },
        "phone": { "type": "string" },
        "address": { "type": "string" },
        "status": { "type": "string" },
        "bookingDate": { "type": "string" },
        "timeSlot": { "type": "string" },
        "totalAmount": { "type": "number" }
      },
      "required": ["id", "labId", "patientName", "phone"]
    },
    "labSettings": {
      "title": "Vendor Lab Settings & CMS Profile",
      "description": "Real-time synchronized laboratory branding, contact info, QR codes, and CMS preferences across all devices",
      "type": "object",
      "properties": {
        "labId": { "type": "string" },
        "labName": { "type": "string" },
        "tagline": { "type": "string" },
        "phone": { "type": "string" },
        "whatsapp": { "type": "string" },
        "email": { "type": "string" },
        "address": { "type": "string" },
        "city": { "type": "string" },
        "upiId": { "type": "string" },
        "upiQrCodeUrl": { "type": "string" },
        "themeColor": { "type": "string" },
        "galleryImages": { "type": "array" },
        "updatedAt": { "type": "string" }
      },
      "required": ["labId", "labName"]
    },
    "labTest": {
      "title": "Laboratory Test Item",
      "description": "Pathology or radiology test with pricing, preparation instructions, and turnaround time",
      "type": "object",
      "properties": {
        "id": { "type": "string" },
        "labId": { "type": "string" },
        "name": { "type": "string" },
        "category": { "type": "string" },
        "price": { "type": "number" },
        "discountPrice": { "type": "number" },
        "tat": { "type": "string" },
        "sampleType": { "type": "string" }
      },
      "required": ["id", "labId", "name", "price"]
    },
    "labPackage": {
      "title": "Health Checkup Package",
      "description": "Preventive and diagnostic health checkup package with bundled tests",
      "type": "object",
      "properties": {
        "id": { "type": "string" },
        "labId": { "type": "string" },
        "name": { "type": "string" },
        "price": { "type": "number" },
        "testsIncluded": { "type": "array" }
      },
      "required": ["id", "labId", "name", "price"]
    },
    "labDoctor": {
      "title": "Lab Doctor & Pathologist",
      "description": "Pathologist or consulting doctor profile associated with laboratory",
      "type": "object",
      "properties": {
        "id": { "type": "string" },
        "labId": { "type": "string" },
        "name": { "type": "string" },
        "designation": { "type": "string" },
        "registrationNumber": { "type": "string" }
      },
      "required": ["id", "labId", "name"]
    },
    "domainRequest": {
      "title": "Vendor Custom Domain Request",
      "description": "Custom domain or subdomain request submitted by laboratory vendor to Super Admin for approval and DNS routing",
      "type": "object",
      "properties": {
        "id": { "type": "string" },
        "labId": { "type": "string" },
        "labName": { "type": "string" },
        "domainType": { "type": "string" },
        "requestedDomain": { "type": "string" },
        "status": { "type": "string" },
        "contactPerson": { "type": "string" },
        "contactPhone": { "type": "string" },
        "createdAt": { "type": "string" }
      },
      "required": ["id", "labId", "requestedDomain", "status"]
    }
  },
  "firestore": {
    "/domain_requests/{requestId}": {
      "schema": { "$ref": "#/entities/domainRequest" },
      "description": "All domain and subdomain requests submitted by vendors for Super Admin review, approval, and DNS binding"
    },
    "/reception_entries/{entryId}": {
      "schema": { "$ref": "#/entities/receptionEntry" },
      "description": "All patient token and billing registrations synchronized in real time across reception and client devices"
    },
    "/lab_reports/{reportId}": {
      "schema": { "$ref": "#/entities/labReport" },
      "description": "Pathology lab reports accessible by technicians, doctors, and patients in real time"
    },
    "/vendor_bookings/{bookingId}": {
      "schema": { "$ref": "#/entities/vendorBooking" },
      "description": "Real-time home collection bookings submitted by clients or receptionists"
    },
    "/lab_settings/{settingId}": {
      "schema": { "$ref": "#/entities/labSettings" },
      "description": "Laboratory profile, branding, contacts, and payment QR codes synced live across all mobile and desktop devices"
    },
    "/lab_tests/{testId}": {
      "schema": { "$ref": "#/entities/labTest" },
      "description": "Test catalog and pricing synchronized across lab devices and client booking page"
    },
    "/lab_packages/{packageId}": {
      "schema": { "$ref": "#/entities/labPackage" },
      "description": "Checkup packages synchronized across devices"
    },
    "/lab_doctors/{doctorId}": {
      "schema": { "$ref": "#/entities/labDoctor" },
      "description": "Doctors and consulting pathologists synchronized across devices"
    }
  }
}
